# Historical DSH adapter reference

This directory preserves the former dependency/workspace configuration for reference. It is not a workspace package and is not installed by the standalone build. For an intact runnable legacy plugin, use git baseline d099d11. Current standalone changes do not promise old DSH plugin installation compatibility.
