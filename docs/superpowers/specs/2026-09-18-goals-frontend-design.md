# 学习目标前端

用户继续授权既定第三阶段。沿用当前工作树和界面，保留所有改动、不提交。

- 导航增加学习目标：进行中/已归档/全部列表，分页；详情读取服务端实时进度，不从列表定义推断达成状态。
- 创建从学习空间已有活动课程选择知识点，输入标题、目标证据分数、不同题目数、明确截止时间。使用浏览器本地时区输入，转换成带时区 UTC 请求，显示当前时区与截止时间。没有课程时提示先在学习空间开始课程，不静默创建课程。
- 详情显示冻结来源/内容版本、截止时间、评估时间、截止前证据统计、最近五份首次答案依据及当前/截止时达标状态。零证据不同于零分；达标不是掌握度，截止前可变化，旧记录可以计入，重复内容不增加独立题目。
- 创建 UUID 和归档 expected_revision 都在发送前持久保存完整请求。结果未知只提供原请求重试，禁止改参数/换编号；确定拒绝后可显式放弃并重读。请求跨页面和刷新恢复，存储失败不发送。归档冲突不自动递增版本。
- 复用 quiz HTTP 适配器和 Core 课程读取；不增加后端规则、提醒、自动出题或模型调用。目标归档独立于截止和进度。

验证：适配器契约、持久请求状态机、组件状态/分页/创建/归档；真实临时 Host，无付费 API；Web 全量、构建、浏览器和 diff 检查。

## 完成状态

已完成，Web 596 项及构建通过。真实服务与浏览器验证记录见同日实施计划。改动保持未提交。
