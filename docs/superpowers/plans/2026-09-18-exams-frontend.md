# 模拟考试前端实施计划

- [x] 类型/API 与可恢复写操作。
- [x] 组卷来源与预览、逐题覆盖审核。
- [x] 考试草稿、倒计时/到期、成绩与报告入口。
- [x] 列表/导航整合、单元/组件与真实 Host 回归。
- [x] 浏览器验收、全量构建测试、diff 与交接更新；保持未提交。


## 验证结果（2026-09-18）

- 最新 `corepack pnpm test`：TypeScript / standalone 构建通过，66 文件 / 608 项通过。
- Quiz 全套：369 passed（53 项既有警告）。考试专项新增普通历史详情读取回归；浏览器复现 nullable summary 导致 500 后，失败测试确认根因，读取边界将 null 转空字符串。适用于已存在考试，不迁移数据，不改变判分或 schema。
- 写操作、组件/API测试覆盖同编号开考恢复、CAS 拒绝、存储失败、多窗口保护、本地草稿恢复、错误草稿校验、多选、不提前展示答案、保存审核后开考、预览条件变化失效、服务端判定到期。
- 真实临时 Host：4 题请求缺 1；3 题可组卷；未审核不能开考；丢失开考响应后只有一次考试；草稿经 Host 重启保留、截止不变；交卷准确率 100%、来源成绩一致、XP=0，报告保持 not_requested，模型调用数不增加。
- Playwright：实际预览缺口、逐题审核、确认开考。1 分钟真实截止验收中保存第一题，第二题仅本地修改；到期后只按保存的一题结算，准确率 33%、未答 2 题、交卷时间等于截止、XP=0。截图 output/playwright/exam-running.png、output/playwright/exam-result.png，已人工查看。
- 修复后另用新临时 home 验证考试成绩进入指定 attempt 报告页并显式生成 fake 报告，分数/XP 不变，控制台 0 errors / 0 warnings。未调用真实收费 API。
- diff check 通过；所有原有修改保留，未提交。未重跑 Core 全套、Electron 打包或插件验证。本轮临时浏览器与服务在收尾时关闭。
