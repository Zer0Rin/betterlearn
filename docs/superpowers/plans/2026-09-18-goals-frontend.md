# 学习目标前端实施计划

按已确认顺序推进第三阶段，保持工作树未提交。

- [x] 类型与 API：列表、详情、创建和归档。
- [x] 持久操作控制器：发送前保存、原请求重试、确定冲突、恢复和写锁。
- [x] 页面：课程来源选择、创建、分页列表、服务端进度详情、归档/恢复。
- [x] 集成：共享页面与主导航、真实 Host 与组件/状态机测试。
- [x] 验证：全量 Web/构建、浏览器、diff 与交接文档。


## 验证记录（2026-09-18）

- 最新 `corepack pnpm test`：TypeScript / standalone 构建通过，64 文件 / 596 项通过。
- API、控制器与组件覆盖创建请求持久化/丢失响应恢复、重复点击、存储失败、归档 CAS 冲突、其他窗口记录保护、分页筛选/迟到响应、没有证据与真实零分、数量不足不能达标、日期合法性及 SQLite UTC 解析。课程在书库外删除时不影响其他有效课程；服务不可用明确报错。
- 真实 Host 验证创建响应丢失后同 UUID 重放仍只有一条目标；未绑定来源的旧练习不计入；关联后交卷三道题，分数 1 但默认五题门槛未达标；归档/恢复与旧 revision 冲突。目标操作不增加 fake provider 调用数。
- Playwright 实际创建目标、刷新读取、归档、恢复；页面显示本地时区截止、100% / 3 of 5 尚未达标、历史依据时间按 UTC 转本地，控制台 0 errors / 0 warnings。截图 output/playwright/goal-progress.png，已人工查看。
- code-review-and-quality 复查与 diff check 通过。保留已有未提交修改；本轮不改后端业务/schema、不重跑 Python 全套或 Electron 打包/插件验证。全部使用临时 home 和 fake provider，无真实收费模型调用。
