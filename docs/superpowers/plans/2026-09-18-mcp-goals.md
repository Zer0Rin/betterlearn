# Learning-goal MCP integration plan

Continue the approved goal backend via four tools: list_learning_goals, read_learning_goal, create_learning_goal and set_learning_goal_archived (betterlearn_ prefix,17 total). This is an adapter over the existing goal contract, no new target/expiry policy or migration. Preserve frontend pause and all workspace work; no installed-plugin updates or real model calls.

- [x] Tests first: strict schemas and read/write annotations, no model configuration/provider/journal, frozen-source create/replay, sanitized rejection vs uncertain dispatch, archive revision never incremented automatically.
- [x] Add dedicated MCP goal adapter using shared Host resolveLearningGoal and owned backend requests. Goal creation dedup lives in Quiz's own namespace; generation journal/UUID behavior unchanged. One attempt per invocation, no automatic retry. Read results and exact archive revision passed through.
- [x] Real official SDK: discover17 tools; create concurrent sameUUID from real Core source, compare HTTP reads, archive/unarchive CAS, conflict, source deletion/restart replay, no extra provider calls. Retain backup/restore coverage.
- [x] Plugin sources/docs: readonly questions never create goals; explicit title/source/deadline for create, clarify timezone when missing; deadlines are not reminders, score not exam grade; uncertain writes reuse exact args, no new UUID/revision. Synchronize three skill files; verify generated configurations with17 tools.
- [x] Independent code/reference scenario review, full Web build/tests, serial plugin verification, diff check and handoff. Python unchanged (prior339 tests not rerun absent a related change).

Verification: Web 51 files / 537 tests passed (MCP 11); TypeScript/standalone build passed. Serial test:plugins passed with 17 tools in both generated configurations. Independent review found no material implementation issues; the stale HTTP-only/13-tool contract wording was corrected. Three plugin skill sources are identical. Python unchanged: prior Quiz 339 passed, not rerun in this stage. No real model calls, daily-home changes, frontend edits, installed-plugin updates or commits.
