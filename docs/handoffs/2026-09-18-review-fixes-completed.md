# 评审问题修复结果（2026-09-18）

基于 c02b341；未提交、未推送。原交接文档保留。未操作日常 BetterLearn home，未调用收费模型。

## 已实施

1. 兼容报告：已有新式轮次时，兼容入口不得新建提交，整个事务拒绝且 HTTP409 原样传到 Host。已有兼容报告只读返回，已交兼容轮次重放使用已保存答案；首次兼容提交维持每卷一次 XP。正式 API 回归比较轮次、答案、题库状态和用户完整快照；真实 Host 回归覆盖409透传及正常轮次交卷/报告。
2. 目标：用户选择按记录交卷时间动态计算。修正页面、指南、契约和插件技能，不引入冻结。正常超时考试的 submitted_at 是考试截止，finalized_at 是实际结算时刻；前者落在目标期限内时，稍后结算仍可更新已截止目标。正式回归覆盖先读取已截止目标、再延迟结算的场景。
3. 空答案：用户选择排除学习证据。统一证据查询排除空选择，覆盖统计、历史、assessment basis 和目标数量门槛；包含旧全卷投影，无需迁移。考试未答计错与题库错题保留。正式回归覆盖漏答、显式清空、重启读历史、幂等交卷、整卷分数及目标。桌面验收改为1/3作答对应一条学习历史。
4. 草稿：按题号与选项排序后比较业务字段，忽略属性顺序与判分附加字段；切换选项保留原 duration_ms。恢复原选择不再误报，真实变化和跨窗口冲突仍可识别。文案区分草稿一致状态与服务端已作答数。
5. 插件：备份后正式重装 Codex，更新 Claude skills-dir 安装并隔离同名 TODO 模板。版本、备份路径和验证见 docs/plugins.md。
6. 次要修正：当前 Quiz schema7 / 升级备份路径文档；保留 schema6 引入目标表的历史事实。空/坏日期显示“时间未知”，避免 null.replace 崩溃。来源规则统一为交卷时冻结归属。

## 验证

- Quiz 全套：375 passed，53 个既有警告；目标测试后续整理后单独复跑26 passed。
- Web 全套：69 文件、626 passed；包含真实 Host 回归。
- TypeScript / standalone build：通过。
- Electron 桌面构建与真实桌面验收：通过（首次运行、生成、目标、审核考试、草稿重启、计分、知识点历史、无自动报告）。首次执行因脚本匹配旧文案失败，同步断言后完整复跑通过。
- 两份实际安装插件：官方 SDK 验证17工具通过；技能 SHA-256 与仓库一致；Codex installed/enabled；Claude manifest 校验通过。
- git diff --check：通过。
- Core 未改，本轮未重跑独立 Core 全套；未重新打包分发安装器。

运行日志副本在 output/verification/review-fixes-2026-09-18/。

## 仍保留的低优先级事项

小写 z 的 due_at 兼容、Host /user/quizzes 分页校验一致性仍沿用原行为。得分精度规则、索引及规模基准、8MiB/16MiB边界没有新增变更；不把未经复现的问题宣称为已修复。新功能继续暂缓。
